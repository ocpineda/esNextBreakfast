/*
 An unfortunate lack of node support...
 */

import { logStrings } from './service.js';

logStrings(
    `Modules?  We don't need no stinkin' modules!`,
    `Oh, I see.  That's kind of nice`,
    `IMPORT/EXPORT ALL THE THINGS!!!`
);
